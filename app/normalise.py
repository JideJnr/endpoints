# predictx/app/normalise.py  (SHIM — deprecated, to be removed in v2)
# noqa: F401  # DEPRECATED shim — see migration_checklist.md
from app.utils.normalise import *  # re-export full public API
from app.utils.normalise import (
    REPLACEMENTS,
    normalise,
)
